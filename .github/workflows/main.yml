name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, evlilik]
    steps:
      - uses: actions/checkout@v4
      - run: |
          ls -la
          cat package.json
